{
    "type": "module",
    "dependencies": {
        "@types/express": "^5.0.3",
        "@types/node": "^24.1.0",
        "express": "^5.1.0",
        "socket.io": "^4.8.1",
        "source-map-support": "^0.5.21"
    },
    "imports": {
        "#root/*": "./build/*"
    },
    "devDependencies": {
        "concurrently": "^9.2.0",
        "typescript": "^5.9.2"
    },
    "scripts": {
        "devtsc": "tsc --watch",
        "devserver": "node --watch --enable-source-maps build/startThisToStartTheGameServer.js",
        "dev": "concurrently --names 'TSC,NODE_SERVER' --prefix '[{name}]' --prefix-colors 'yellow,cyan' 'pnpm:devtsc' 'pnpm:devserver'"
    }
}
